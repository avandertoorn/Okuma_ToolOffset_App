<UserControl x:Class="ToolOffset_Application.Views.Navigation.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolOffset_Application.Views.Navigation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=local:NavigationViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <StackPanel>

            <Button Content="Turret" 
                    Command="{Binding Path=TurretViewNavigation}"/>

            <Button Content="Block" 
                    Command="{Binding Path=BlockViewNavigation}"/>

            <Button Content="Tool"
                    Command="{Binding Path=ToolViewNavigation}"/>

        </StackPanel> 
    </Grid>
</UserControl>
